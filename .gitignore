# Binaries
/engram
/dist/
*.exe

# Go
*.test
*.out
vendor/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Node (landing-page Tailwind build tooling; built CSS in www/ is committed)
node_modules/

# Claude Code personal settings (verbose/debug, env)
.claude/settings.local.json

# Secrets / local env — never commit (context7 etc. read keys from the shell env)
.env
.env.*
!.env.example
*.pem
*.key
